"""Approval presentation constants (no imports — avoid circular deps)."""

from __future__ import annotations

APPROVAL_TOKEN_KWARG = "_yasin_approval_token"
APPROVAL_PRESENT_ENV = "YASIN_MCP_PRESENT_APPROVAL_TOKEN"
