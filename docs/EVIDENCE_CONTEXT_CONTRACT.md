# Agent Context Retrieval and Evidence Contract

**Status:** CONFIRMED for typed representation (Issue #43).

Flow: Query → retrieval → sources → evidence classification → AgentContextBundle.

Inferences are never labeled CONFIRMED solely by proximity to sources.

See `yasin_mcp.contracts.evidence_context`.
